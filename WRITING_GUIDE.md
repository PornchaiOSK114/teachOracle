# ✍️ คู่มือเขียนบทความ — teedba.com

คู่มือนี้เขียนให้ **อาจารย์ตี๋** ใช้ตอนนั่งเขียนบทความจริง ไม่ต้องรู้เรื่องโค้ดก็ทำตามได้

> ทุกอย่างในคู่มือนี้ทดสอบกับเว็บจริงแล้วว่าใช้ได้ ไม่ใช่ตัวอย่างลอย ๆ

---

## 📂 1. บทความเก็บไว้ที่ไหน

```
content/articles/
├── read-execution-plan.mdx        ← 1 ไฟล์ = 1 บทความ
├── rman-backup-strategy.mdx
└── _template-execution-plan.mdx   ← ไฟล์ตัวอย่าง (ขึ้นต้นด้วย _ ไว้ให้ดูโครง)
```

**ชื่อไฟล์คือ URL** — ไฟล์ `read-execution-plan.mdx` จะกลายเป็น
`https://teedba.com/articles/read-execution-plan`

กติกาชื่อไฟล์:

- ใช้ **อังกฤษพิมพ์เล็ก ตัวเลข และขีดกลาง** เท่านั้น
- ห้ามมีภาษาไทย เว้นวรรค หรือ underscore
- ตั้งให้สื่อความและมีคำค้นที่คนน่าจะพิมพ์หา เช่น `oracle-rman-backup-strategy` ดีกว่า `post-01`

---

## 🚀 2. สร้างบทความใหม่

### วิธีที่ 1 — ใช้คำสั่ง (แนะนำ)

เปิด Terminal ที่โฟลเดอร์โปรเจ็ค แล้วพิมพ์

```bash
npm run new:post read-execution-plan "อ่าน Execution Plan ให้เป็นใน 10 นาที"
```

จะได้ไฟล์พร้อมหัวไฟล์ครบ เปิดขึ้นมาเขียนเนื้อหาต่อได้เลย

### วิธีที่ 2 — ก๊อปไฟล์ตัวอย่าง

ก๊อป `content/articles/_template-execution-plan.mdx` ไปตั้งชื่อใหม่ แล้วเขียนทับ

### ดูผลงานระหว่างเขียน

```bash
npm run dev
```

เปิด `http://localhost:3000` — แก้ไฟล์แล้วหน้าเว็บอัปเดตทันทีไม่ต้องรีเฟรช
และโหมดนี้ **เห็นบทความ draft ด้วย**

---

## 📋 3. หัวไฟล์ (Frontmatter) — ส่วนบนสุดที่คั่นด้วย `---`

```mdx
---
title: "อ่าน Execution Plan ให้เป็นใน 10 นาที"
description: "วิธีอ่านแผนการทำงานของ Oracle Optimizer ทีละขั้น พร้อมตัวอย่างจริงที่ใช้ได้ทันที"
tldr: "เริ่มจากดูแผนจริงด้วย DBMS_XPLAN.DISPLAY_CURSOR แล้วเทียบ E-Rows กับ A-Rows ถ้าต่างกันมากแปลว่าสถิติล้าสมัย"
category: "Performance"
tags: ["SQL", "Tuning", "Execution Plan"]
date: "2026-07-26"
cover: "/images/blog/execution-plan.jpg"
draft: true
---
```

| ช่อง | จำเป็น | ความหมาย |
| :--- | :---: | :--- |
| `title` | ✅ | ชื่อบทความ — โผล่บนหน้าเว็บ, แท็บเบราว์เซอร์, และผลค้นหา Google |
| `description` | ✅ | สรุปสั้น ~150 ตัวอักษร — โผล่บนการ์ดและใต้ชื่อในผลค้นหา Google |
| `tldr` | ✅ | **สรุปคำตอบ 1–2 ประโยค** โผล่เป็นกล่องแดงต้นบทความ · ดูหัวข้อ 8 ว่าทำไมสำคัญ |
| `category` | ✅ | เลือก **1 หมวด** จากรายการด้านล่าง |
| `tags` | – | คำค้น ใช้จับคู่ "บทความที่เกี่ยวข้อง" ท้ายหน้า |
| `date` | ✅ | `YYYY-MM-DD` เท่านั้น — ผิดรูปแบบเว็บจะ build ไม่ผ่านและบอกชื่อไฟล์ให้ |
| `cover` | – | รูปปก ไม่ใส่ก็ได้ (จะใช้แบนเนอร์ตัวเลขแทน) |
| `draft` | – | `true` = ยังไม่ขึ้นเว็บจริง · เขียนเสร็จเปลี่ยนเป็น `false` |

### หมวดที่ใช้ได้ (พิมพ์ให้ตรงเป๊ะ)

`DBA` · `Performance` · `Backup & Recovery` · `PL/SQL` · `RAC` · `Oracle Linux` · `Data Engineer` · `Data Science` · `SQL`

> อยากเพิ่มหมวดใหม่ ให้แก้ที่ `lib/site.ts` ตัวแปร `categories`
> หมวดที่ยังไม่มีบทความจะไม่ขึ้นเป็นปุ่มกรอง เว็บจึงไม่มีปุ่มที่กดแล้วว่างเปล่า

### ระบบฉบับร่าง (Draft)

| สถานะ | `npm run dev` | เว็บจริง |
| :--- | :---: | :---: |
| `draft: true` | ✅ เห็น | ❌ ไม่เห็น |
| `draft: false` | ✅ เห็น | ✅ เห็น |

เขียนค้างไว้กี่ไฟล์ก็ได้ ตราบใดที่ยังเป็น `draft: true` จะไม่มีใครเห็น

---

## 📝 4. เขียนเนื้อหา — Markdown พื้นฐาน

```mdx
ย่อหน้าปกติ พิมพ์ต่อกันไปได้เลย เว้นบรรทัดว่าง 1 บรรทัดเพื่อขึ้นย่อหน้าใหม่

## หัวข้อใหญ่ (จะขึ้นในสารบัญอัตโนมัติ)

### หัวข้อย่อย (ไม่ขึ้นสารบัญ)

**ตัวหนา** และ *ตัวเอียง* และ `โค้ดสั้น ๆ ในบรรทัด`

- รายการแบบจุด
- อีกข้อ

1. รายการแบบตัวเลข
2. อีกข้อ

> ข้อความอ้างอิง จะขึ้นเป็นกล่องพื้นอ่อนมีเส้นแดงด้านซ้าย
```

<!-- prettier-ignore -->
> ⚠️ **ห้ามใช้ `#` (H1) ในเนื้อหา** — ระบบใส่ชื่อบทความเป็น H1 ให้แล้ว
> ถ้าใส่ซ้ำจะเสีย SEO เริ่มจาก `##` เสมอ

**สารบัญขึ้นอัตโนมัติ** เมื่อมีหัวข้อ `##` ตั้งแต่ 2 อันขึ้นไป ไม่ต้องทำเอง

---

## 💻 5. แทรกโค้ด SQL / PL/SQL

ใส่ชื่อภาษาต่อท้าย backtick 3 ตัว เพื่อให้ระบายสีถูกต้อง

````mdx
```sql
SELECT e.employee_id, e.last_name
  FROM hr.employees e
 WHERE e.salary > 10000;
```
````

ภาษาที่ใช้บ่อยในเว็บนี้: `sql` · `plsql` · `bash` · `text` (ไม่ระบายสี เหมาะกับผลลัพธ์คำสั่ง)

<!-- prettier-ignore -->
> 💡 โค้ดยาวเกินหน้าจอจะเลื่อนซ้ายขวาได้เอง แต่ควรตัดบรรทัดให้พอดี ~80 ตัวอักษร
> เพราะคนอ่านบนมือถือเยอะ

**โค้ดสั้น ๆ ในประโยค** ใช้ backtick เดี่ยว เช่น `` `DBMS_XPLAN` `` จะได้ `DBMS_XPLAN`

---

## 📊 6. ตาราง

```mdx
| คอลัมน์ | ความหมาย | หมายเหตุ |
| :--- | :--- | ---: |
| `E-Rows` | จำนวนที่ประเมิน | ชิดซ้าย |
| `A-Rows` | จำนวนจริง | ชิดขวา |
```

`:---` = ชิดซ้าย · `:---:` = กึ่งกลาง · `---:` = ชิดขวา

ตารางกว้างเกินจอมือถือจะเลื่อนซ้ายขวาได้เอง

---

## 🖼️ 7. รูปภาพ

### วางไฟล์ไว้ที่ไหน

```
public/images/blog/execution-plan.jpg
```

แล้วอ้างถึงด้วย path ที่ **ตัด `public` ออก** → `/images/blog/execution-plan.jpg`

### แบบง่าย (ไม่มีคำบรรยาย)

```mdx
![รายงาน AWR ส่วน Top 10 Foreground Events](/images/blog/awr.png)
```

ข้อความในวงเล็บเหลี่ยมคือ **alt text** — สำคัญมาก เพราะคนตาบอดใช้โปรแกรมอ่านหน้าจอ
และ Google ใช้เข้าใจว่ารูปคืออะไร **อย่าเว้นว่าง อย่าเขียนว่า "รูปภาพ"**

### แบบมีคำบรรยายใต้ภาพ

```mdx
<Figure
  src="/images/blog/awr.png"
  alt="รายงาน AWR ส่วน Top 10 Foreground Events"
  caption="ส่วนที่ควรดูก่อนเสมอเมื่อเปิด AWR Report"
/>
```

<!-- prettier-ignore -->
> 💡 ระบบย่อรูปและแปลงเป็น WebP/AVIF ให้อัตโนมัติ อัปโหลดไฟล์ใหญ่ได้เลย
> แนะนำกว้างอย่างน้อย 1200px จะได้คมบนจอ Retina

---

## 🔗 8. ลิงก์

```mdx
[ข้อความลิงก์](https://docs.oracle.com)              ← เปิดทับแท็บเดิม
[บทความอื่นในเว็บเรา](/articles/rman-backup)         ← ลิงก์ภายใน ใช้ path สั้น

<ExternalLink href="https://docs.oracle.com">เอกสารทางการ Oracle</ExternalLink>   ← เปิดแท็บใหม่
```

<!-- prettier-ignore -->
> 💡 **ลิงก์ไปบทความอื่นในเว็บเราเองช่วย SEO มาก** — Google เข้าใจว่าเนื้อหาเชื่อมโยงกัน
> และคนอ่านอยู่ในเว็บนานขึ้น พยายามใส่อย่างน้อย 1–2 ลิงก์ต่อบทความ

---

## 🎬 9. ฝังวิดีโอและเสียง

### YouTube

```mdx
<YouTube id="dQw4w9WgXcQ" title="สาธิตการอ่าน Execution Plan" />
```

**หา `id` ยังไง** — ดูจาก URL ของวิดีโอ

```
https://www.youtube.com/watch?v=dQw4w9WgXcQ
                                └──── นี่คือ id ────┘
https://youtu.be/dQw4w9WgXcQ
                 └──── นี่คือ id ────┘
```

### Vimeo

```mdx
<Vimeo id="123456789" title="ติดตั้ง Oracle RAC ทีละขั้น" />
```

`id` คือตัวเลขท้าย URL: `https://vimeo.com/123456789`

### SoundCloud

```mdx
<SoundCloud url="https://soundcloud.com/ชื่อผู้ใช้/ชื่อแทร็ก" />
```

ใส่ URL เต็มของแทร็กได้เลย

### ไฟล์เสียงของเราเอง (MP3)

วางไฟล์ที่ `public/audio/ep01.mp3` แล้วเขียน

```mdx
<Audio src="/audio/ep01.mp3" title="พอดแคสต์ตอนที่ 1 — เริ่มต้นเป็น DBA" />
```

<!-- prettier-ignore -->
> 💡 วิดีโอทุกตัวโหลดแบบ lazy (โหลดเมื่อเลื่อนถึง) และจองพื้นที่ไว้ล่วงหน้า
> หน้าเว็บจึงไม่กระตุกและไม่ช้าลง
>
> YouTube ใช้โดเมน `youtube-nocookie.com` และ Vimeo ใส่ `dnt=1`
> เพื่อไม่ให้วาง cookie ติดตามผู้อ่าน — ตรงกับที่เว็บนี้ไม่ต้องมีแบนเนอร์ขอความยินยอม

---

## 💬 10. กล่องเน้นข้อความ (Callout)

```mdx
<Callout>
เนื้อหาเคล็ดลับทั่วไป จะขึ้นกล่องสีแดงอิฐพร้อมไอคอน 💡
</Callout>

<Callout type="warning" title="ระวังบน Production">
การเก็บสถิติใหม่อาจทำให้ SQL อื่นที่เคยเร็วกลับช้าลง
</Callout>

<Callout type="danger">
คำสั่งนี้ลบข้อมูลถาวร กู้คืนไม่ได้
</Callout>
```

| `type` | สี | ใช้เมื่อ |
| :--- | :--- | :--- |
| ไม่ใส่ (tip) | แดงอิฐ 💡 | เคล็ดลับ ข้อสังเกต |
| `warning` | ส้ม ⚠️ | ข้อควรระวัง ทำแล้วอาจมีผลข้างเคียง |
| `danger` | แดง 🛑 | อันตราย ทำแล้วแก้ไม่ได้ |

ใส่ `title="..."` เพื่อเปลี่ยนหัวข้อกล่องได้

<!-- prettier-ignore -->
> ⚠️ ต้อง **เว้นบรรทัดว่าง** ระหว่าง `<Callout>` กับเนื้อหาข้างใน
> ไม่งั้น Markdown ข้างในจะไม่ทำงาน

---

## 🎯 11. เขียนยังไงให้ติด Google และถูก AI หยิบไปตอบ

นี่คือส่วนที่ทำให้เว็บนี้ต่างจากบล็อกทั่วไป — เราออกแบบมาเพื่อ **3 อย่างพร้อมกัน**

| ย่อ | คือ | หมายถึง |
| :--- | :--- | :--- |
| **SEO** | Search Engine Optimization | ติดอันดับ Google |
| **GEO** | Generative Engine Optimization | ถูก ChatGPT / Claude / Perplexity หยิบไปตอบ |
| **AIO** | AI Optimization | AI เข้าใจเนื้อหาเราถูกต้อง |

### กฎข้อที่ 1 — `tldr` คือช่องที่สำคัญที่สุด

เมื่อมีคนถาม AI ว่า *"Oracle SQL ช้าเพราะอะไร แก้ยังไง"* AI จะไปอ่านเว็บต่าง ๆ
แล้วหยิบ **ประโยคที่ตอบคำถามตรงที่สุด** มาตอบพร้อมอ้างอิงกลับมาที่เว็บต้นทาง

ช่อง `tldr` คือที่ที่เราวางประโยคนั้นไว้ให้ AI เจอง่ายที่สุด

✅ **เขียนแบบนี้**

```yaml
tldr: "เริ่มจากดูแผนจริงด้วย DBMS_XPLAN.DISPLAY_CURSOR แล้วเทียบ E-Rows กับ A-Rows ถ้าต่างกันมากแปลว่าสถิติล้าสมัย ให้รัน DBMS_STATS.GATHER_TABLE_STATS ใหม่"
```

❌ **อย่าเขียนแบบนี้**

```yaml
tldr: "บทความนี้จะพาไปดูวิธีการอ่าน Execution Plan"
```

อันแรก **ตอบคำถาม** อันหลัง **แค่บอกว่าจะเล่าอะไร** ซึ่ง AI หยิบไปใช้ไม่ได้

### กฎข้อที่ 2 — ย่อหน้าแรกต้องตอบคำถามเลย

อย่าเกริ่นว่า *"ในโลกของฐานข้อมูลปัจจุบัน..."* คนอ่านไทยกดปิดใน 5 วินาที
และ AI ก็หาคำตอบไม่เจอ **ตอบก่อน แล้วค่อยอธิบายเหตุผล**

### กฎข้อที่ 3 — ตั้งหัวข้อ `##` เป็นคำถามหรือประโยคที่ค้นหาจริง

| ✅ ดี | ❌ ไม่ดี |
| :--- | :--- |
| `## Full Table Scan ไม่ดีเสมอไปหรือเปล่า` | `## ข้อสังเกต` |
| `## วิธีเก็บสถิติใหม่ด้วย DBMS_STATS` | `## ขั้นตอนที่ 3` |

เพราะคนพิมพ์ค้นด้วยคำแบบนี้จริง ๆ และ AI ใช้หัวข้อเป็นตัวจับว่าย่อหน้านั้นตอบเรื่องอะไร

### กฎข้อที่ 4 — `description` เขียนให้คนอยากคลิก

`description` คือบรรทัดที่โผล่ใต้ชื่อในผลค้นหา Google บอกให้ชัดว่า **อ่านแล้วได้อะไร**

| ✅ ดี | ❌ ไม่ดี |
| :--- | :--- |
| "วิธีอ่าน Execution Plan ทีละขั้น พร้อมตัวอย่าง SQL ที่ก๊อปไปใช้ได้ทันที" | "บทความเกี่ยวกับ Execution Plan" |

### กฎข้อที่ 5 — ใส่คำที่คนค้นหาจริงลงในเนื้อหา

คนไทยค้นทั้งไทยและอังกฤษปนกัน เช่น *"oracle จูน sql ช้า"*
ควรมีทั้งคำว่า `SQL Tuning`, `จูน SQL`, `ช้า`, `Execution Plan` อยู่ในบทความอย่างเป็นธรรมชาติ

<!-- prettier-ignore -->
> ⚠️ **อย่ายัดคำค้นซ้ำ ๆ** Google จับได้และลดอันดับ เขียนให้คนอ่านรู้เรื่องเป็นหลักเสมอ

### กฎข้อที่ 6 — เขียนจากประสบการณ์จริง

Google เรียกเกณฑ์นี้ว่า **E-E-A-T** (Experience, Expertise, Authoritativeness, Trust)
เว็บนี้ได้เปรียบอยู่แล้วเพราะมี JSON-LD ระบุว่าผู้เขียนเป็น OCP ประสบการณ์ 20+ ปี

สิ่งที่ช่วยเพิ่มอีก: เล่าเคสจริงที่เจอ ("ครั้งหนึ่งลูกค้ารายหนึ่ง...") ใส่ตัวเลขจริง
บอกข้อจำกัดตรง ๆ ว่าวิธีนี้ใช้ไม่ได้เมื่อไหร่ — สิ่งเหล่านี้ AI คัดลอกจากที่อื่นไม่ได้

---

## ✅ 12. เช็กก่อนเผยแพร่

- [ ] `title` ชัดเจน มีคำที่คนค้นหา
- [ ] `description` เขียนให้คนอยากคลิก ~150 ตัวอักษร
- [ ] **`tldr` ตอบคำถามตรง ๆ ไม่ใช่แค่บอกว่าจะเล่าอะไร**
- [ ] `date` รูปแบบ `YYYY-MM-DD`
- [ ] `category` พิมพ์ตรงกับรายการ
- [ ] ย่อหน้าแรกตอบคำถามเลย ไม่เกริ่นยาว
- [ ] มีหัวข้อ `##` อย่างน้อย 2 อัน (สารบัญจะได้ขึ้น)
- [ ] ไม่มี `#` (H1) ในเนื้อหา
- [ ] รูปทุกใบมี alt text ที่บรรยายจริง
- [ ] code block ระบุภาษาแล้ว
- [ ] มีลิงก์ไปบทความอื่นในเว็บอย่างน้อย 1 ลิงก์
- [ ] เปิด `npm run dev` ดูจริงแล้วไม่มีอะไรเพี้ยน
- [ ] **ลองย่อหน้าต่างเบราว์เซอร์ให้แคบเท่ามือถือ ดูว่ายังอ่านง่าย**
- [ ] เปลี่ยน `draft: true` → `false`

---

## 🚀 13. นำขึ้นเว็บ

**double-click ไฟล์ `2-อัปเดตเว็บ.bat`** ในโฟลเดอร์โปรเจ็ค แล้วพิมพ์สรุปสั้น ๆ ว่าแก้อะไร

หรือถ้าถนัด Terminal

```bash
git add -A
git commit -m "เพิ่มบทความ: อ่าน Execution Plan"
git push origin main
```

รอประมาณ 1–2 นาที Vercel จะ build เสร็จและบทความขึ้นเว็บ

**ไม่ต้องทำอะไรเพิ่ม** — sitemap, RSS, llms.txt และหน้ารวมบทความอัปเดตให้เองทั้งหมด

<!-- prettier-ignore -->
> 💡 Google ใช้เวลาหลายวันถึงหลายสัปดาห์กว่าจะเก็บบทความใหม่เข้าระบบ
> ถ้าอยากเร่ง ให้เข้า [Search Console](https://search.google.com/search-console?resource_id=sc-domain:teedba.com)
> → URL inspection → วาง URL บทความ → กด **Request indexing**

---

## 🆘 14. เจอปัญหา

| อาการ | สาเหตุที่พบบ่อย | วิธีแก้ |
| :--- | :--- | :--- |
| บทความไม่ขึ้นบนเว็บจริง | ยังเป็น `draft: true` | เปลี่ยนเป็น `false` แล้ว push ใหม่ |
| Vercel build ไม่ผ่าน | `date` ผิดรูปแบบ หรือลืม `title` | ดูข้อความ error จะบอกชื่อไฟล์ที่ผิด |
| Callout ข้างในไม่เป็นตัวหนา/ลิงก์ | ไม่ได้เว้นบรรทัดว่าง | เว้นบรรทัดว่างหลัง `<Callout>` |
| รูปไม่ขึ้น | path ผิด (ใส่ `public` มาด้วย) | ใช้ `/images/...` ไม่ใช่ `/public/images/...` |
| วิดีโอไม่ขึ้น | ใส่ URL เต็มแทน id | `<YouTube id="dQw4w9WgXcQ" />` ไม่ใช่ URL ทั้งอัน |
| สารบัญไม่ขึ้น | มีหัวข้อ `##` แค่อันเดียว | ต้องมีตั้งแต่ 2 อันขึ้นไป |

---

## 📚 เอกสารอื่น

- `README.md` — โครงสร้างโปรเจ็ค ชุดสี คำสั่งทั้งหมด
- `docs/ANALYTICS.md` — ดูสถิติคนเข้าเว็บและคำค้นที่คนใช้หาเรา
- `AGENTS.md` — สำหรับ AI agent ที่มาช่วยพัฒนาต่อ
